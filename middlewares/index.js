const controllerWrapper = require('./controllerWrapper')
const validation = require('./validation')
const authenticate = require('./authenticate')
const authenticateContact = require('./authenticateContact')

module.exports = {
  controllerWrapper,
  validation,
  authenticate,
  authenticateContact
}
